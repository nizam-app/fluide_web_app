# Flunexia web app — Vite environment
# Copy to `.env` for local development.

# API (same for staging and production web)
VITE_API_URL=https://api.flunexia.fr/api/v1
# Local API alternative:
# VITE_API_URL=http://localhost:5000/api/v1

# Deploy URLs (build is hosted separately — set in hosting / DNS):
# Staging (current):  https://staging.flunexia.fr
# Production (later): https://flunexia.fr
